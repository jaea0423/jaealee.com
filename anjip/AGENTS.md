# anjip/ — 작업 지침 (Codex·GPT 용)

**먼저 `README.md` 를 읽으세요.** 한옥반점 사이트(`../hanok/`)를 바탕으로 만든 안집(맞은편 한식당) 사이트입니다. 정적 파일, 빌드 없음.

- 글·사진·차림·영업시간은 전부 `data/site.js`. **`(확인)` 이 붙은 값은 고모께 아직 못 받은 임시값**입니다 — 마음대로 채우지 말고 그대로 두세요.
- **가격은 화면에 쓰지 않습니다**(`INFO.showPrices = false`, 고모 뜻). 메뉴판 PDF 로만.
- 온라인 예약 없음 — '예약' 단추는 전부 전화(`tel:`)입니다. 예약 창을 붙이지 마세요.
- 약도(`visit.html`)는 한옥반점 것과 같은 그림이고 **주인공(안집)만 흰색**입니다. 한쪽을 고치면 `../hanok/visit.html` 도 같이 고칩니다.
- CSS 는 한옥반점 복사본 + 맨 아래 "안집" 구역(갈색 테마). 위쪽 공용 부분을 고치면 한옥반점과 달라지니 조심하세요.
- 확인: 저장소 루트에서 `python -m http.server 8768` → `http://127.0.0.1:8768/anjip/`. 폭 430 / 820 / 1280.
- 끝내면 `../hanok/system/work/PROGRESS.md` 에 한 줄 남깁니다.
