# hanok/ — 작업 지침 (Codex·GPT 용)

**이 폴더의 규칙은 같은 폴더 `CLAUDE.md` 에 전부 있습니다. 고치기 전에 먼저 읽으세요.**
`hanok/` = 손님이 보는 가게 사이트(정적, 빌드 없음), `hanok/system/` = 예약 관리 시스템(빌드 필요, 규칙 다름 → `system/AGENTS.md`).

아래는 읽지 않고 손대면 사고가 나는 것만 추린 것입니다.

## 절대 규칙

- `test/hanok/` 은 **보관용**입니다. 사장님께 나간 링크가 살아 있어 옮기거나 지우지 않습니다.
- 사이트 글·사진·영업시간 기본값은 `data/site.js`. 서버(`site_versions`)가 이걸 덮어씁니다 — 화면에 안 보인다고 코드를 고치기 전에 서버 값을 의심하세요.
- 예약 창(`js/reserve.js`)의 `resCode()` 는 시스템의 같은 함수와 **글자 하나까지 같아야** 합니다(예약번호 8자리). 한쪽만 고치지 마세요.
- **알러지 칸은 없습니다**(2026-09-20). 알러지는 요청사항에 같이 적습니다. 되살리지 마세요.
- 더미·예시 이름은 실존 인물 대신 **만화·영화·애니메이션 인물**을 씁니다.
- `supabase.*.json`, PIN, API 키는 어떤 파일·커밋 메시지에도 새로 쓰지 않습니다.

## 확인

- `python work/shot.py` (8767 포트 서버가 떠 있어야 함) 또는 브라우저로 직접.
- 폭 네 가지: 430(폰) / 820(태블릿) / 1280(PC) / 1920.
- 추측으로 "됐을 겁니다" 하지 말고 **화면을 보고** 확인합니다.

## 끝낼 때

- `hanok/system/work/PROGRESS.md` 에 한 줄 남깁니다(날짜 · 무엇을 · 왜).
- 요청한 파일만 골라 커밋합니다. 무관한 변경을 같이 담지 않습니다.
