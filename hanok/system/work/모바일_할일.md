# 모바일(폰 390px) — 2026-09-17 전면 손봄

폰 전용 규칙은 전부 `src/css/12b-mobile.css` 한 곳에(≤767px). 검토는 `REVIEW_W=390 python work/review_shots.py sys` 로 전 화면(66장)을 찍어 `work/review/sheets/` 에서 봤습니다.

## 한 것
- 타임라인 층 테이블 줄(6칸)이 너무 높음 → **3칸으로 접고 층 이름을 누르면 펼침**(`tlToggleFloor`, "3칸만 ▾ / 숨은 N팀"). 접힌 동안 못 들어간 팀은 안 그리고 수만 표시
- 대시보드 지표 칩 5개가 가로 스크롤이라 '확인' 칩이 잘린 줄 몰랐음 → 두 줄로 감쌈
- 마법사 단계 점 7개가 안 들어가 7번째가 잘림 → 점을 촘촘히. 발밑 경고가 버튼을 찌그러뜨림 → 경고 윗줄·버튼 아랫줄. 날짜 단계 영업시간 줄 '라스트오더' 잘림 → 줄바꿈
- 목록 행(홈페이지 예약·검색·확인 목록) 둘째 줄이 한 줄 말줄임으로 세 글자만 남음 → 두 줄까지, 좌석 알약은 셋째 줄로
- 설정 폴드가 목록 아래에서 열려 내용이 화면 밖 → 열면 머리를 위로 끌어올림(`toggleFold` + scroll-margin). 설정 한 줄의 점선·오른쪽 정렬이 한 칸 격자에서 어수선 → 점선 숨김·왼쪽 정렬·입력은 100%
- 홈페이지 관리: 영업시간 줄 요일/시각/× 가 세 줄 → 요일 한 줄 + '시각 ×' 한 줄. 미리보기 머리 '새 창' 이 두 줄로 접힘 → 제목 숨기고 알약만
- 확인해 보니 이미 괜찮았던 것: 노쇼율 세 칸 · 특정 테이블 지정 셀 · 좌석 폴드 테이블/합침 탭 · 임시 일정 일괄 등록 · 문자 기록 · PIN·비밀번호 시트 · 손님 화면(폰은 '화면이 작습니다' 안내)

## 남은 것 / 실기기에서 볼 것
- 예약률 추이 그래프(3주)가 폰에서 빽빽함 — 2주로 줄일지 판단
- 검토 도구(헤드리스 크롬) 캡처에서 FAB '+' 아이콘이 가끔 안 찍히는데 브라우저에서는 정상 — 실기기 확인
- 실기기(사장님 폰)에서 마법사 7단계·설정·홈페이지 관리 한 바퀴
