# v7 진행 기록

- 2026-09-15 v7 시작: v6 복사 → `src/index.html` 을 `page.html` + `css/00~13` + `js/00~19` 로 나눔(`work/split.py`, 배너 기준). `build.py` 가 이름 순으로 이어 붙임 — dev 빌드가 v6 와 빈 줄만 다름. `patchlib` v7(조각 단위), CLAUDE.md
- 2026-09-15 정리 1·2: 중복 minToHM, 안 부르는 함수 24개, 직원·근태·매출 주석 코드 전부 삭제(js 8,360→7,691줄), 죽은 CSS 135규칙 삭제(2,542→2,367줄). 렌더 전수(대시보드·설정·시트 20종·마법사·TV) 오류 0. 보안 점검표 `work/전수조사.md`(S1~S12: 결정 필요 S6 캐시 범위·S7 PIN 자리수·S9 로그 보관)
- 2026-09-15 정리 3: 같은 셀렉터 덮어쓰기 중 결과가 같음이 보장되는 12건 합침(`work/merge_css.py` — 사이 규칙이 같은 요소·같은 속성을 못 건드릴 때만). 나머지 덮어쓰기(52 셀렉터)는 `12-overrides.css`(v3 디자인 개편)에 모여 있고 순서가 곧 우선순위 — 디자인 단계에서 손댐. 화면 17장(1280/820/430) v6 빌드와 픽셀 단위 동일 확인(`work/shots/v6_ref` vs `v7b_after`)
