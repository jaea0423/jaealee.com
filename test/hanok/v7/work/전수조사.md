# v7 전수조사 — 2026-09-15 시작

## 1. 구조 정리 (끝)

| 항목 | 결과 |
|---|---|
| 파일 나누기 | `src/index.html` 10,955줄 → `page.html` + css 14조각(2,367줄) + js 18조각(7,691줄). 빌드 결과는 v6 와 같음 |
| 중복 정의 | `minToHM` 2번 → 앞 것(24시간 넘김 처리 없음) 삭제 |
| 안 부르는 함수 | 24개 삭제: addRole·delRole·moveRoom·setRoomField·setSettingNum(옛 설정), changeLines, dayBadge·renderRes·resCard·renderSeatBoard·setDate·weekStart(옛 예약 탭·좌석 보드), floorLeft·floorLoad·stayMin·won, goHome, openSetLog, ratePreview, setTheme·toggleTheme(테마 토글 삭제됨), smsSeat·toggleClosed, setStatus(카드 상태 버튼 — 지금은 수정 시트·markRes) |
| 주석 코드 | 직원·근태·매출(비활성) 주석 덩어리 전부 삭제 — `12-staff-off.js` `13-sales-off.js` 파일째, `09-sms.js` 매출 그래프, `15-sheets.js` 직원·근태·매출 입력 시트. 데이터 슬롯(`staff·attendance·sales` 배열)은 옛 저장본 호환으로 남김 |
| 죽은 CSS | 135규칙 삭제 + 6규칙에서 죽은 셀렉터 제거(`work/clean_css.py`). 남긴 것: `tv-bg.b1~b7`(동적), `tvx-legacy`(자리), `sb-mock`(흉내 표시 규칙) |
| 확인 | 대시보드 5개 날짜·설정 폴드 11개·시트 20종·운영시간 편집·마법사 7단계 렌더 오류 0, TV 목록형 정상 |

## 2. 보안

| # | 항목 | 상태 | 비고 |
|---|---|---|---|
| S1 | anon 키만 파일에 | ✓ | `build.py` 가 service_role 이면 빌드 중단. anon 키·URL 은 공개돼도 RLS 가 막음 |
| S2 | RLS | ✓ | `hanok_is_staff()` — staff/admin 이메일 두 계정만, 익명 토큰 거부(S3). DELETE 권한 회수(S4, soft delete). logs 는 update/delete 불가 |
| S3 | 공개 뷰 | ✓ | `public_today`: 마스킹 이름·시각·인원·좌석·상태·희망 층만(오늘). `public_screen`: rooms·displayRows·tvAd·tvType·tvIdleFull 만. 전화·요청·메모·알러지 안 나감 |
| S4 | XSS | ✓ | 화면에 넣는 사용자 글자는 `esc()`(이름·전화·요청·메모·알러지·경로·좌석 이름·네이버 붙여넣기·일괄 등록·설명 팝업). 확인창(`renderModal`)은 `msg` 를 통째로 esc. `innerHTML` 직접 대입 12곳 확인 — 전부 esc 거침 또는 고정 문자열 |
| S5 | 세션 토큰 | △ | access/refresh 토큰을 `sessionStorage` 에 (탭 닫으면 삭제). XSS 가 없는 한 안전. 만료 시 refresh — `sb()` 401 처리 확인됨 |
| S6 | 오프라인 캐시 | △ | `localStorage` 에 예약 전체(이름·전화) 평문. 매장 태블릿 분실 시 노출. **실서비스 전 결정**: 캐시를 ±14일로 줄일지, 기기 잠금(OS)에 맡길지 |
| S7 | PIN | △ | 화면 4자리 + 서버 비밀번호 PIN+"00". 클라이언트 잠금(틀리면 대기)은 있지만 서버 쪽은 Supabase Auth 기본 속도 제한만. 4자리 = 10,000가지. **실서비스 전**: dev PIN 교체(대화에 노출됨), 관리자 비밀번호 별도(이미), 필요하면 PIN 6자리 |
| S8 | 오프라인 PIN 해시 | △ | `localStorage` 에 PIN 해시(strHash) — 기기를 가진 사람은 10,000번 시도로 풀 수 있음. 풀어도 그 기기의 캐시 열람뿐(서버는 못 씀). S6 과 같은 결정 |
| S9 | 접속 기록 | △ | logs 에 이름·전화(노쇼 취소 등) 적힘. 직원 계정이면 볼 수 있음. 복사는 관리자 비밀번호. 실서비스에서 개인정보 보관 기간(예: 90일 뒤 삭제)을 SQL cron 으로 둘지 결정 |
| S10 | 로그인 계정 | ✓ | 사인업 꺼짐(체크리스트) 전제. 이메일 두 개 하드코딩은 RLS 함수 안 — prod 프로젝트에서 이메일 바꿔 실행 필요(`patch_8차.sql`) |
| S11 | 외부 요청 | ✓ | Supabase 외 외부 요청 0 (글꼴·이미지 내장). 광고 영상은 같은 폴더 |
| S12 | 손님 TV | ✓ | 로그인 없이 공개 뷰만. 관리 화면 주소는 TV 에서 열리지 않게 PIN 필요 |

## 3. 오류 (정적)

| 항목 | 결과 |
|---|---|
| node --check | 통과 |
| 구형 문법 검사 | 통과 (`?.` `??` `.flat` `.replaceAll` `structuredClone` 0건) |
| 정의 없이 부르는 이름 | 0 (calc/gradient/repeat/var 는 CSS 문자열 오탐) |
| 같은 셀렉터 3번 이상 정의 | 57개(`work/audit_css.py`) — 덮어쓰기 통합은 4장 |

## 4. 남은 일 (순서)

1. CSS 덮어쓰기 통합 — `12-overrides.css`(v3 1차·디자인 개편 덮어쓰기 215줄)와 `11-media.css` 를 원래 규칙 자리로 합치기. 한 셀렉터씩, 스크린샷 비교
2. 시나리오 재점검(v6 에서 한 것 v7 에서 다시)
3. S6·S7·S9 결정(재아) → 반영
4. 디자인
