v4/CLAUDE.md 를 읽고, 이어서 v4/work/7차_작업지시.md 를 처음부터 끝까지 읽어. ../docs/supabase_준비.md 는 필요할 때만 참고해.
그 문서의 "9. 묶음과 멈춤 지점" 대로 묶음 A부터 시작해. 문서에 적힌 결정(테이블 구조·충돌 처리·권한·오프라인·개발용 프로젝트·fetch 직접 호출·백업)은 다시 묻지 말고, 적히지 않은 것이 애매하면 멈추고 물어.
규칙 우선순위: ../CLAUDE.md 2장 > 7차_작업지시.md > apple-design 스킬.
src/index.html 전체를 Read 하지 마. grep 으로 위치를 찾아 구간만 읽어. 예약 객체의 모양은 바꾸지 마 — 행↔객체 변환은 rowToRes/resToRow 두 함수 안에서만.
service_role 키는 어떤 파일에도 쓰지 마. prod 프로젝트에는 전부 끝날 때까지 아무것도 쓰지 마.
묶음 하나 끝날 때마다 `cd v4 && python build.py dev` 통과, 문서에 적힌 확인 항목을 실제 브라우저(Playwright, dev 프로젝트)에서 돌린 결과, work/PROGRESS.md 한 줄, 그리고 멈춰서 보고해. 묶음 A가 끝나면 내가 Supabase 대시보드 작업(checklist 1~5)을 하고 "다음" 이라고 할 때까지 기다려.
