# 홈페이지 작업 지침

- 사용자가 요청한 홈페이지 변경을 완료하면 변경 범위에 맞게 검증한 뒤 커밋과 푸시까지 진행합니다. 동일 작업 범위에서 매번 재확인하지 않습니다.
- 실제 변경이 없거나 검증에 실패한 경우에는 불필요한 커밋·푸시를 하지 않습니다. 사용자가 미리보기만 요청하거나 게시하지 말라고 하면 해당 요청을 우선합니다.
- 요청한 작업 파일만 명시적으로 선택해 커밋합니다. 기존 사용자 변경이나 무관한 파일을 함께 포함하지 않습니다.
- 강제 푸시는 하지 않습니다. 인증·충돌 등으로 푸시하지 못하면 완료 상태와 필요한 조치를 정확히 알립니다.
- 커밋·푸시 성공과 실제 사이트 배포 완료는 구분해서 보고합니다.

## 두 컴퓨터와 자동 발행

- 작업 시작 전에 Git 상태와 원격 변경을 확인합니다. 깨끗한 작업 폴더는 fetch 후 fast-forward로 동기화하며, 기존 변경을 자동 stash/reset하지 않습니다.
- 아이맥 자동 발행은 automation/README.md와 DAILY.md를 따릅니다. 자동화 전용 복제본에서 당일 데이터·목록만 수정하고 UI 파일을 건드리지 않습니다.
- Windows에서는 화면·기능을 주로 수정하고 실행 중인 자동화의 당일 데이터·목록과 동시 수정을 피합니다.
- 푸시가 거절되면 원격 변경을 먼저 확인합니다. 겹치지 않는 파일만 최신 이력에 다시 적용하고 검증합니다. 같은 파일 충돌을 임의로 한쪽 선택하거나 강제 푸시하지 않습니다.

- 사용자는 아이맥 매일 06:00(Asia/Seoul) 자동 조사·작성·검증·커밋·푸시를 확정했습니다. 정상 실행에 매번 승인을 요청하지 않습니다.
- 콘텐츠 발행일은 한국시간 07:00~익일 07:00 직전입니다. home/edition-day.js를 공통으로 사용하고 실제 행사·날씨·시세 시각은 변경하지 않습니다.

- 아이맥 생성 대상 날짜는 06:00 경계의 preparationDate, 화면 노출은 07:00 경계의 date를 사용합니다. 기존 날짜 JSON은 불변, 세 목록 JSON에는 새 항목만 추가합니다.

## 폴더 정리와 공유 주소 보호

- `/test`는 사용자가 외부에 공유한 경로입니다. 별도 명시 요청 없이 하위 파일·작업 폴더를 포함해 이동·이름 변경·삭제하지 않습니다.
- 공개 웹 폴더와 자동화 실행 폴더는 기존 주소·상대경로를 유지합니다. 프로젝트 구조는 루트 `README.md`를 참고합니다.
- 사용 중단이 확인된 파일은 `_disabled/`에 기존 내부 구조를 보존해 옮기고, `_disabled/README.md`에 원래 위치와 사유를 기록합니다. 미참조 파일이라는 이유만으로 비활성화하지 않습니다.
- 페이지 실행과 무관한 새 기획·설명 문서는 `docs/projects/`에 둡니다.
